name: restoration
channels:
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - torch==2.11.0+cu130
      - torchvision==0.26.0+cu130
      - --extra-index-url https://download.pytorch.org/whl/cu130
      - numpy
      - pillow
      - tqdm
      - einops
      - pyyaml
      - tensorboard
      - matplotlib
      - pandas
      - scikit-image
      - ruff
